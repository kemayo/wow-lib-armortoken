[tool.poetry]
name = "libarmortoken-tools"
version = "0.1.0"
description = "Datamining tools for LibArmorToken-1.0"
authors = ["David Lynch <kemayo@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.32.3"
requests-cache = "^1.2.1"
pyyaml = "^6.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
